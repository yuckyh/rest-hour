services:
 dynamodb-hotel:
   command: "-jar DynamoDBLocal.jar -sharedDb -dbPath ./data"
   image: "amazon/dynamodb-local"
   container_name: dynamodb-hotel
   ports:
     - "8000:8000"
   volumes:
     - "./data:/home/dynamodblocal/data"
   working_dir: /home/dynamodblocal
